{
  "name": "proxy-test",
  "description": "Testing the behaviour of Keystone cookies behind reverse proxies",
  "private": true,
  "version": "1.0.0",
  "author": "The KeystoneJS Development Team",
  "license": "MIT",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development DISABLE_LOGGING=true keystone dev",
    "build": "NODE_ENV=production keystone build",
    "start": "NODE_ENV=production keystone start"
  },
  "dependencies": {
    "@keystonejs/adapter-knex": "^7.0.0",
    "@keystonejs/app-admin-ui": "^5.9.3",
    "@keystonejs/app-graphql": "^5.1.4",
    "@keystonejs/auth-password": "^5.1.3",
    "@keystonejs/fields": "^7.0.2",
    "@keystonejs/keystone": "^7.0.0"
  }
}
